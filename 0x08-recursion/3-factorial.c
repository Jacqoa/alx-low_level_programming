#include "main.h"

/** factorial - factorial
 *@n: number to factorized
 *
 *Return: integer
 */
int factorial(int n)
{
	if (n < 0)
		return (0);
	if (N <= 0)
		return (1);
	return (n *factorial(n - 1));
}
